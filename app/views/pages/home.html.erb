<main role="main" class="inner cover">
  <h1 class="cover-heading"><%= t('home.title') %></h1>
  <p class="lead"><%= t('home.description') %></p>
  <p class="lead">
    <%= link_to t('home.about_me.title'), about_path, class: "btn btn-secondary" %>
  </p>
</main>
