<div class="col-md-4">
  <div class="position-sticky" style="top: 2rem;">
    <div class="p-3 mb-3 bg-secondary text-white">
        <h4 class="fst-italic"><%= t('blogs.blog_about.title') %></h4>
        <p class="mb-0"><%= t('blogs.blog_about.description') %></p>
    </div>

    <div>
      <%= source_helper("alert alert-info p-4") %>
    </div>

    <div class="p-3">
      <h4 class="fst-italic"><%= t('blogs.topics')%></h4>
      <% @sidebar_topics.each do |topic| %>
        <%= link_to topic.title, topic_path(topic) %>
        <br>
      <% end %>
    </div>

    <div class="p-3">
        <h4 class="fst-italic"><%= t('blogs.social_medias') %></h4>
        <div class="social-links">
            <a class="me-2 text-dark" href="https://github.com/julianogm"><%= fa_icon "github" %></a>
            <a class="me-2 text-dark" href="https://linkedin.com/in/julianoagm "><%= fa_icon "linkedin-square" %></a>
            <a class="me-2 text-dark" href="#"><%= fa_icon "instagram" %></a>
            <a class="me-2 text-dark" href="#"><%= fa_icon "facebook-square" %></a>
            <a class="me-2 text-dark" href="#"><%= fa_icon "twitter" %></a>
        </ol>
    </div>
  </div>
</div>